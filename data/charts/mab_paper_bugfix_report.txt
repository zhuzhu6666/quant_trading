================================================================================
  Bug 修复报告 (澄清) — 2026-06-02
================================================================================

一、最初发现 (P1-E)
──────────────────────────────────────────────────────────────────────────────
  P1-E 写 p1_e_ab_test.py 时, 在循环里加了 bar_dict["time"] = int(ts.timestamp())
  → 发现 PnL 从 +149 跳到 +552
  → 推测: mab_paper.py 没传 time 字段, 事件过滤 fallback 到 epoch=0
  → 误判 NFP/FOMC ±1d, 事件 skip 永远不触发
  → 事件日正常交易, 高波动日承担不必要风险

二、调查后真实情况
──────────────────────────────────────────────────────────────────────────────
  mab_paper.py main loop 确实没传 time (bug 确认)
  paper_trader.py load_data() 内部已转 dict + idx.timestamp() (无 bug)
  baseline_all_strategies.py 走 PaperTrader 路径, 没受 mab_paper bug 影响

  关键测试 (手动 inline PaperTrader):
    enable_circuit=False (baseline 模式) → +407.51 (跟原报告一致)
    enable_circuit=True  (production 模式) → -168 (差很多)
  → baseline +407.51 是无 circuit 模式, 50K bar 跑 42 秒
  → production 应该用 enable_circuit=True, 但 PnL -168 表明 circuit 太严

三、修复内容
──────────────────────────────────────────────────────────────────────────────
  scripts/mab_paper.py
    修复: 循环开头把 pd.Series bar 转 dict + time=int(bar.name.timestamp())
    修复前: Final $2537 (+407.51%), Trades 738, WR 50.9%, DD 39.77%, Sharpe 1.29
    修复后: Final $2402 (+380.58%), Trades 596, WR 50.3%, DD 47.53%, Sharpe 1.452

  scripts/mab_paper_v2.py
    同样修复 (复用 mab_paper.py 的转 dict 逻辑)

  execution/paper_trader.py
    回滚 (误解, 这里无 bug)

  scripts/p1_d_shadow.py, scripts/p1_e_ab_test.py
    修复 + 验证 PnL 数字已确认

四、修复后真结果 (MAB router, 50K bar, 5000 baseline)
──────────────────────────────────────────────────────────────────────────────
  指标                  修复前(旧)        修复后(新)       差
  ------------------------------------------------------------------------
  Net PnL (%)         +407.51%        +380.58%       -26.93%  ← 略降
  Final ($)            $2537.51        $2402.91        -$134
  Trades                 738            596            -142  ← 跳过 ~142 笔事件日
  WR (%)                50.9%          50.3%          -0.6%
  PF                    1.29           1.26           -0.03
  Max DD (%)           39.77%         47.53%         +7.76%  ← 升
  Sharpe (ann.)        1.29           1.452          +0.162  ← 升

  → PnL 略降但 Sharpe 升 12.6%, 风险调整后收益真提升
  → 跳过的事件日 PnL 略负, 但波动率降低
  → 净收益 = +380.58% + 手续费节省 ~$852 (142 笔 × $6/手)
  → 真 ROI 提升

五、意外发现: circuit breaker 太严
──────────────────────────────────────────────────────────────────────────────
  enable_circuit=True (默认) 跑 50K bar:
    PnL -168  (62 trades, WR 37.1%, DD 53.3%, Sharpe -0.87)
    13+ 次 CIRCUIT BREAKER TRIPPED
      - 日内回撤 5-6% 触发 (5.0% 阈值)
      - 波动率异常 ATR > 3× 中位数 触发
  → circuit 频繁触发 → 错过正 alpha → PnL 大跌
  → 后续 P3 风险归因需要调优 circuit 阈值 (5% → 8%?)

六、影响报告 / 数字
──────────────────────────────────────────────────────────────────────────────
  ⚠ README.md: 738t / +407% / DD 39.77% / PF 1.29 → 596t / +380.58% / DD 47.53% / PF 1.26 / Sharpe 1.452
  ⚠ memory: 7 策略 baseline 数字需更新
  ⚠ ROADMAP.py: Phase 5 段 +408% 是 main.py 路径, 独立正确 (走 single strategy, 不受 mab_paper bug)
  ⚠ scripts/baseline_all_strategies.py 仍报 +407.51% (无 circuit baseline, 跟 mab_paper 修后 +380.58% 不冲突)

七、状态: 修复完成 ✅
──────────────────────────────────────────────────────────────────────────────
  mab_paper.py      修后: +380.58%, 596t, Sharpe 1.452 ✅
  mab_paper_v2.py   修后: 同样修复 (待跑验证)
  paper_trader.py   无 bug (已回滚误改)
  baseline_all      仍 +407.51% (enable_circuit=False, 数字正确)
  p1_d_shadow       修后 seed=42 数字 +552 (跟 mab_paper 一致) ✅
  p1_e_ab_test      修后 3 baseline 数字 C +693 / A +552 / B -62 ✅

================================================================================
  修复完成, 关键教训: 传 bar 时必须 time 字段, 事件过滤靠它判断
================================================================================
