[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    postgres_integration: requires an isolated PostgreSQL service
filterwarnings =
    ignore:datetime\.datetime\.utcfromtimestamp\(\) is deprecated.*:DeprecationWarning:google\.protobuf\.internal\.well_known_types
